
/// <reference types="vite/client" />

// Import the ElectronAPI interface from our types file
import { ElectronAPI } from './types/electron';

// No need to redefine ElectronAPI here since we're importing it
// This avoids duplicate and potentially conflicting definitions

// The global window interface extension is already in the electron.d.ts file
